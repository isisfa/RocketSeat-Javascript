//Cada caso será testado, se não for encontrado retornará default:

let expression = 'c'

switch (expression) {
    case 'a':
        console.log('é a')
        break
    case 'b': 
        console.log('é b')
        break
    default:
        console.log('não encontrado')
        break
}

